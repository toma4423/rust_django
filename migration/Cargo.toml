[package]
name = "migration"
version = "0.1.0"
edition = "2021"

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
async-trait = "0.1"
sea-orm-migration = { version = "1.1.0", features = [
  "runtime-tokio-rustls",
  "sqlx-postgres",
] }
